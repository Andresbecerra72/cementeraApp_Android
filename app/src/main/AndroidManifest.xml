<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="app.absoftware.cementera">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".popups.PopupChart"
            android:theme="@style/Theme.TemaPopup"/>
        <activity android:name=".popups.PopupRefA"
            android:theme="@style/Theme.TemaPopup"/>
        <activity android:name=".MenuMain" />
        <activity android:name=".SignUp" />
        <activity android:name=".RecoverPass" />
        <activity android:name=".GraphicChart" />
        <activity android:name=".FormData" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>